'use strict';
//
// Use case mainApp controller: StartViewCtrl
// 
angular.module('mainApp').controller('StartViewCtrl', 
        ['$scope','$window', '$state', 'transferSrv', 'MainSrv', '$stateParams', 
            function ($scope, $window, $state, transferSrv, MainSrv, $stateParams) {
				$scope.data = {
					
				};
				
			}]);
